{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && pnpm install --filter=web... --frozen-lockfile --prefer-offline",
  "buildCommand": "pnpm --filter=web build",
  "outputDirectory": ".next"
}
